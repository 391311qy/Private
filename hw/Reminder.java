class Reminder {
    public static void main (String[] operands) {
        System.out.println (Integer.parseInt(operands[0]) 
                            % Integer.parseInt(operands[1]));
    }
}

// Integer.parseInt method converts a string of decimal digits to an integer.